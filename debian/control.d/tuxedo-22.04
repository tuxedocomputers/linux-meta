Package: linux-headers-tuxedo${variant:suffix}
Architecture: amd64 armhf arm64 ppc64el s390x
Section: kernel
Depends: ${misc:Depends}, linux-headers-${kernel-abi-version}-tuxedo
Description: TUXEDO Linux kernel headers
 This package will always depend on the latest TUXEDO kernel headers
 available.

Package: linux-image-tuxedo${variant:suffix}
Architecture: amd64 armhf arm64 ppc64el s390x
Section: kernel
Provides: ${dkms:v4l2loopback-modules} ${dkms:zfs-modules} ${dkms:ivsc-modules} ${dkms:virtualbox-guest-modules} ${dkms:wireguard-linux-compat-modules}
Depends: ${misc:Depends}, linux-image-${kernel-abi-version}-tuxedo, linux-modules-extra-${kernel-abi-version}-tuxedo [amd64 arm64 ppc64el s390x], linux-firmware [amd64 armhf arm64 ppc64el], intel-microcode [amd64], amd64-microcode [amd64]
Recommends: thermald [amd64]
Description: TUXEDO Linux kernel image
 This package will always depend on the latest TUXEDO kernel image
 available.

Package: linux-image-uc-tuxedo${variant:suffix}
Architecture: amd64 arm64
Section: kernel
Depends: ${misc:Depends}, linux-image-uc-${kernel-abi-version}-tuxedo, linux-modules-extra-${kernel-abi-version}-tuxedo, linux-firmware, firmware-sof-signed [amd64], wireless-regdb
Description: TUXEDO Linux kernel image for Ubuntu Core
 This package will always depend on the latest TUXEDO kernel image
 available.
 .
 This kernel is suitable for staging into a kernel snap, and is not
 suitable to be used directly on classic systems.

Package: linux-tools-tuxedo${variant:suffix}
Architecture: amd64 armhf arm64 ppc64el s390x
Section: kernel
Provides: linux-tools
Depends: ${misc:Depends}, linux-tools-${kernel-abi-version}-tuxedo
Description: TUXEDO Linux kernel tools
 This package will always depend on the latest TUXEDO kernel tools
 available.

Package: linux-cloud-tools-tuxedo${variant:suffix}
Architecture: amd64
Section: kernel
Provides: linux-cloud-tools
Depends: ${misc:Depends}, linux-cloud-tools-${kernel-abi-version}-tuxedo
Description: TUXEDO Linux kernel cloud tools
 This package will always depend on the latest TUXEDO kernel cloud tools
 available.

Package: linux-tuxedo${variant:suffix}
Architecture: amd64 armhf arm64 ppc64el s390x
Section: kernel
Provides: ${test:provides-full-tuxedo} ${test:provides-full-preferred}
Depends: ${misc:Depends}, linux-image-tuxedo${variant:suffix} (= ${binary:Version}), linux-headers-tuxedo${variant:suffix} (= ${binary:Version})
Recommends: linux-tools-${kernel-abi-version}-tuxedo, ubuntu-kernel-accessories
Description: Complete TUXEDO Linux kernel and headers
 This package will always depend on the latest complete TUXEDO Linux kernel
 and headers.

